const nomes = ["Lily", "Nay", "Julia", "Marcos", "Phill", "Luan"];

export function aleatório (lista)
  const posicao = Math.floor(Matha.random()* lista.lenght);
  return lista[posição];
}

export const nomes = aleatorio(nomes)
